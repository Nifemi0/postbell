# Extractable components

The vanilla frontend currently has no source-level shared components suitable for Superdesign component extraction. The page shell and controls are written inline in each HTML document. Skip component extraction for this target.

