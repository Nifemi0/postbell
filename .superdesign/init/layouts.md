# Shared layouts

There is no shared layout component. `src/public/index.html` contains the original terminal shell. `src/public/postbell.html` contains the standalone visual-prototype shell. Each document owns its header, navigation, columns, and content hierarchy.

